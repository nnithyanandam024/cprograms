#include<stdio.h>
#include<string.h>
int main()
{
    char str1[200],str2[200];
    scanf("%[^\n]",str1);
    getchar();
    scanf("%[^\n]",str2);
    if(strstr(str1,str2))
    {
        printf("Substring found!");
    }
    else {
        printf("Substring not found!");
    }
}
