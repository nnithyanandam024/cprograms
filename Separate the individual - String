#include<stdio.h>
#include<string.h>
int main()
{
    char str[200];
    int i=0;
    scanf("%[^\n]",str);
    while(str[i]!='\0')
        {
            printf("%c ",str[i]);
            i++;
        }
}
